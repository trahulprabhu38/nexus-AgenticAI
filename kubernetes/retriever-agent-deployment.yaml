apiVersion: apps/v1
kind: Deployment
metadata:
  name: retriever-agent
  labels:
    app: retriever-agent
    tier: agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: retriever-agent
  template:
    metadata:
      labels:
        app: retriever-agent
        tier: agent
    spec:
      containers:
        - name: retriever-agent
          image: trahulprabhu38/nexus-retriever_agent:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8007
          envFrom:
            - configMapRef:
                name: nexus-config
            - secretRef:
                name: nexus-secrets
          volumeMounts:
            - name: retriever-data
              mountPath: /app/sample_data
          readinessProbe:
            httpGet:
              path: /health
              port: 8007
            initialDelaySeconds: 90
            periodSeconds: 15
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8007
            initialDelaySeconds: 120
            periodSeconds: 30
            failureThreshold: 5
          resources:
            requests:
              memory: "512Mi"
              cpu: "300m"
            limits:
              memory: "1Gi"
              cpu: "750m"
      volumes:
        - name: retriever-data
          persistentVolumeClaim:
            claimName: retriever-data-pvc

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: retriever-data-pvc
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

---
apiVersion: v1
kind: Service
metadata:
  name: retriever-agent
  labels:
    app: retriever-agent
spec:
  selector:
    app: retriever-agent
  ports:
    - port: 8007
      targetPort: 8007
  type: ClusterIP
