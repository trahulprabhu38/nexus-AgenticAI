apiVersion: v1
kind: Service
metadata:
  name: sql-generator
  labels:
    app: sql-generator
spec:
  selector:
    app: sql-generator
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP
